# Steganography
This project implements image-based steganography using C, where secret text is hidden inside an image using the Least Significant Bit (LSB) technique. It supports embedding and extracting data through binary file handling, demonstrating secure data hiding using bitwise operations.
